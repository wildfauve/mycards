<div class="cd-create-error">
	<% if card.errors.any? %>  
	    <div id="errorExplanation">  
	      <h2><%= pluralize(card.errors.count, "error") %> prohibited this record from being saved:</h2>  
	      <ul>  
		      <% card.errors.messages.each do |field, msg| %>  
		      	<li><%= "#{field}: #{msg[0]}" %></li>  
		      <% end %>  
	      </ul>  
	    </div>  
	<% end %>
</div>