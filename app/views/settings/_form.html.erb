<%= form_for [@config, @setting] do |form| %>
	<div class="field">
		<%= form.text_field :name, :placeholder => "Name" %>
	</div>
	<div class="field">
		<%= form.text_field :value, :placeholder => "value" %>
	</div>

	<div>
		<p>
			<%= form.submit "Submit", :class => "small-button" %>
		</p>
	</div>
	
<% end %>
